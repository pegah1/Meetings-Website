module.exports = function(grunt) {
    'use strict';

    // Config
    grunt.initConfig({
        closurecompiler: {
            minify: {
                files: {
                    'build/jquery.jqflipswitch.min.js': ['build/jquery.jqflipswitch.js']
                },
                options: {
                    'compilation_level': 'SIMPLE_OPTIMIZATIONS'
                }
            }
        },
        watch: {
            files: ['build/jquery.jqflipswitch.js'],
            tasks: ['closurecompiler:minify']
            /*
            options: {
                livereload: true
            }
            */
        }
    });

    // Plugins
    grunt.loadNpmTasks('grunt-closurecompiler');
    grunt.loadNpmTasks('grunt-contrib-watch');

    // Tasks
    grunt.registerTask('minify',  ['closurecompiler:minify']);
    grunt.registerTask('default', ['closurecompiler:minify', 'watch']);
};
